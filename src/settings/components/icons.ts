import { setIcon } from "obsidian";

export function plusIcon(el: HTMLElement) {
  setIcon(el, "plus");
}

export function xIcon(el: HTMLElement) {
  setIcon(el, "x");
}

export function trashIcon(el: HTMLElement) {
  setIcon(el, "trash");
}

export function circleSmallIcon(el: HTMLElement) {
  setIcon(el, "circle-small");
}

export function pencilIcon(el: HTMLElement) {
  setIcon(el, "pencil");
}

export function chevronUpIcon(el: HTMLElement) {
  setIcon(el, "chevron-up");
}

export function chevronDownIcon(el: HTMLElement) {
  setIcon(el, "chevron-down");
}
